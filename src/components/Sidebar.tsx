const Sidebar = () => {
  return (
    // (h-screen is just to force overscroll, isn't needed)
    <div className="p-4 h-screen">
      <p>Sidebar (T.B.D.)</p>
    </div>
  );
};
export default Sidebar;
